import styled from "styled-components";
import { space, borderRadius, maxHeight, maxWidth } from "styled-system";

const Image = styled.img`\
  ${maxHeight}
  ${maxWidth}
  ${space}
  ${borderRadius}
`;

export default Image;
